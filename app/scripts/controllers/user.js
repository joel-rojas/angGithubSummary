'use strict';

/**
 * @ngdoc function
 * @name gitHubTestApp.controller:UserCtrl
 * @description
 * # UserCtrl
 * Controller of the gitHubTestApp
 */
angular.module('gitHubTestApp')
  .controller('UserCtrl', function (webService) {
    
  });
